<!DOCTYPE html>
<html>
<nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark">
    <span class="navbar-brand mb-0 h1">
    <a class="navbar-brand" href="/Tuto">Tuto</a></span>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/movies">Films<span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/cast_members">Cast Members</a>
      </li>
      <% if current_user.try(:admin?) %>
        <li class="nav-item">
          <a class="nav-link" href="/accounts">Users Administration</a>
        </li>
      <%end %>


    </ul>

    <% if current_user.try(:admin?) %>
      <span class="navbar-brand mb-0 h1" style="color: #fe365e">Admin mode</span>
    <%end %>

    <% if user_signed_in? %>
      <%= link_to current_user.email, edit_user_registration_path, :class => "btn btn-outline-light my-2 my-sm-0"  %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => "btn btn-outline-light my-2 my-sm-0"  %>
    <% else %>
      <%= link_to "Sign up", new_user_registration_path, :class => "btn btn-outline-light my-2 my-sm-0"  %> |
      <%= link_to "Login", new_user_session_path, :class => "btn btn-outline-light my-2 my-sm-0"  %>
    <% end %>



  </div>

</nav>
</html>